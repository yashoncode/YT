package com.yt.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * YouTube's own membership green. Live chat readers recognise members and moderators by this exact
 * colour across every client, so it is a brand constant rather than a theme role.
 */
val LiveChatMemberGreen = Color(0xFF0B8043)

/** The tint behind a membership announcement row. */
val LiveChatMemberSurface = Color(0xFF0F9D58).copy(alpha = 0.18f)
